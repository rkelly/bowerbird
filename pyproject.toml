[project]
name = "bowerbird"
version = "0.1.0"
description = "A suite of data-only tools for SQL Server"
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy>=2.0",
    "pyodbc>=5.0",
]

[project.scripts]
copy-table = "copy_table:main"
compare-rowcounts = "compare_rowcounts:main"
